//fifteenth method
matchMethod="Busca un patrón que encaje con regexp y devuelve un Array con las coincidencias encontradas. null si no se encuentran. El método .matchAll() devuelve un iterador para poder recorrer las coincidencias encontradas."
console.log(matchMethod.match("que"))